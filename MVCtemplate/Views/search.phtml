<?php require('template/header.phtml') ?>
<h3>Results:</h3>

<?php 

    echo '<div id="paginationLinks">';
        for($i=1; $i<=$view->numberOfPages; $i++){
            echo '<form method="post" action="search.php?page=' . $i . '"><input class="searchPaginationInput" type="submit" name="paginationButton" value="' . $i . '"><input type="hidden" value="' . $view->userSearchTerm . '" name="userSearchTerm"></form>';
        }
    echo '</div>';
    
?>

<div class="row">
    <?php 
        foreach($view->paginatedSearchResults as $data) {

            $gamePrice = "£" . $data->getPrice();
            if($gamePrice == "£0"){
                $gamePrice = $gamePrice . ".00";
            }
            echo '
            <div class="width-23 mx-auto my-1 p-0 text-center bg-bl radius-border font-xs">
                <div class="row font-white glow-border m-0 p-0">
                    <div class="col-sm-6 my-1 p-0">
                        <p>ID</p>
                        <p class="font-blue font-bold">'.$data->getAppID().'</p>
                    </div>
                    <div class="col-sm-6 my-1 p-0">
                        <p>Status</p>
                        <p class="font-blue font-bold">'.$data->getStatus().'</p>
                    </div>
                    <div class="col-sm-12 my-1 p-0">
                        <p>Units available</p>
                        <p class="font-blue font-bold">'.$data->getUnitsAvailable().'</p>
                    </div>
                    <div class="col-sm-12 my-1 p-0">
                        <p>Units sold</p>
                        <p class="font-blue font-bold">'.$data->getUnitsSold().'</p>
                    </div>
                    <div class="w-75 col-sm-12 mx-auto py-2 d-flex justify-content-between border-top">
                        <span>Price</span>
                        <span class="font-blue font-bold">'.$gamePrice.'</span>
                    </div>
                </div>
            </div>';
            
        }
    ?>
    </div>


<?php require('template/footer.phtml') ?>