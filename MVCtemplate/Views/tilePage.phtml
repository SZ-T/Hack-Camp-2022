<?php require('Views/template/header.phtml'); ?>
<div id="full"></div>
<div class="row justify-content-md-center m-0 animate__animated animate__fadeInUp animate__delay-07s">
        <?php
            //var_dump(isset($_POST["filter-form"]));
            //var_dump($_POST["filter-form"]);
            //var_dump($_POST["fromDate"]);
            foreach($view->gameDataSet as $gameData) {
                tile($gameData);
            }
        ?>
    </div>
<?php 

require('Views/template/footer.phtml');

if (isset($LazyLoad)){
    echo '<script>
        LazyLoad.init("'.$LazyLoad->url.'", "'.$LazyLoad->type.'", ["'.implode('", "', $array).'"]);
        LazyLoad.loadResults();
    </script>';
}

?>